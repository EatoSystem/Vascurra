# Vascurra production web assets

Keep reference screenshots out of this tree. Only ship assets used by the site.

```text
public/vascurra/
  brain/          hero / medium / header WebP of the Vascurra brain
  brand/          reserved for lockups
```

The current hero WebP is a high-resolution production derivative of the approved brain mark (1024×1024, ~255 KB). A true 4096 master from the original brand pack is still preferred when it becomes available — swap the files in `components/brand/brain-assets.ts`.
